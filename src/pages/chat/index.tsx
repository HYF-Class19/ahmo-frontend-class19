import React from 'react';
import {NextPage} from "next";
import MainLayout from "@/layouts/MainLayout";

const Chat: NextPage = () => {
    return (
        <MainLayout>
            Hello
        </MainLayout>
    );
};

export default Chat;
